<% layout('layout') -%>


    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <img src="<%= product.image %>" alt="">
            </div>

            <div class="col-md-4">
            </div>

            <div class="col-md-4">
                <h2>
                    <%= product.name %>
                </h2>
                <h1>
                    $
                    <%= product.price %>
                </h1>
                <p>
                    <%= product.description %>
                </p>
            </div>
        </div>
    </div>
